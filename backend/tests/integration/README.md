/**
 * Integration Tests — Maverik Store Backend
 *
 * ⚠️  Thư mục này dành cho integration tests (Supertest + real Express app).
 *
 * Chưa triển khai trong bước này.
 * Xem testing_audit.md để biết kế hoạch phase tiếp theo.
 *
 * Yêu cầu trước khi thêm integration test:
 * - Tách src/server.ts → src/app.ts (export Express app không listen)
 * - Cấu hình test database riêng biệt
 */
